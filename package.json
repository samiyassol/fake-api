{
  "name": "fake-store-api",
  "version": "1.0.0",
  "description": "Fake Store API using JSON Server on Render",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
